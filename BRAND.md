# 品牌故事 — zmzai.cloud 知末智云

> 一套给个人创作者与独立开发者用的、自托管的 AI 应用生态：智能体编排、
> 模型调度、受限沙箱、单点登录与统一设计系统。本文是 `zmzai.cloud` 枢纽站
> 与所有子项目共用的品牌事实来源。锁定于 2026-08-05，修订于 2026-08-25。

---

## 0. 一句话

`zmzai.cloud` 是知末智云的云。它不是一个产品，是一个品牌盖在一打可独立
部署的工程上。每件工程都能独立运行，但它们共享同一套身份、数据模型与视觉
语言，都姓知末智云。

## 1. 为什么要有这个品牌

市面上不缺 AI 站。缺的是"看得见作者"的那种站，缺的是"数据握在自己手里"
的那种底座。

打开一个典型的 AI 生成站，你看到的是：紫色渐变 hero、Inter 字体从标题灌到
正文、四张图标方块功能卡、居中对齐的口号、荧光绿或靛蓝的强调色。它没有作者，
它有一个 prompt。

`zmzai.cloud 知末智云` 是对这个问题的回答。它把"牧之"这个作者署名从 logo
里拿出来，放回它该在的位置：**署名**。logo 主体交给 `zmzai` 这个可被占用的、
罗马化的词；"牧之"作为署名，盖在 muzhi 站和每篇内容的末尾。像作者在书末
签字，而不是像 SaaS 在导航栏贴图标。

## 2. 名字的三层

```
zmzai.cloud        ← 域名 / 枢纽身份（hub）
└── 知末智云        ← 品牌 / 平台主体
    └── 牧之        ← 作者署名（muzhi 站、文章落款）
```

- **`zmzai`** 是 handle，罗马化的、可被占用的词。它是「知末」拼音首字母
  z·m 与「AI」的合成。它做 wordmark，用等宽字体敲出来，像铭牌。它是枢纽站
  的脸。
- **`cloud`** 是载体。知末智云的云。不强调"云"的技术含义，强调"这是知末智云
  的一片地方，里面装着他做的工程"。
- **`知末智云`** 是品牌全称，中文世界里的身份。知末——知其末梢，追到系统
  的末节；智云——智能在云端。四个字的拼音首字母 z·m·z·y 与产品矩阵的
  z·m·z·a·i 同源（末位是 AI）。Slogan：**知其末梢，智在云端。**
- **`牧之`** 是作者署名。它出现在 muzhi 站、每篇内容末尾、方印里。它不当
  logo 主体——当 logo 主体就成了又一个方块字标。当署名，它就有作者权重。

这三个词的层级关系本身就是品牌系统：枢纽（zmzai.cloud）承载品牌（知末智云）
的作品集（子项目），作者（牧之）在作品上署名。

## 3. 品牌主张

**作者在场，数据自持。**

- 不是"AI 帮你生成了知识站"，是"牧之搭了一套底座，你能看见他怎么搭的、
  为什么这么搭"。
- 不是"多租户 SaaS"，是"你自己部署、你自己握数据和权益"。知末智云把这套
  东西开源出来，是因为相信创作者该握住自己的内容、交易和用户关系。
- 不是"工具聚合站"，是"一条经过真实业务验证的闭环：发布 → 信任 → 权益 →
  交付 → 反馈 → 改进"。

品牌不和"AI"这个词打架，它和"AI 生成的、看不见作者"打架。

## 4. 视觉骨架（与 @zmzai/theme 对应）

视觉方向是**纯白底 + 墨黑结构 + 绿色语义**，代号「墨骨 Ink Frame」，
由 `@zmzai/theme` v0.8.0 统一提供，全站共用，禁止在项目里自造品牌资产。

**核心原则：结构色与语义色分离。** 呼应第 1 节——我们批判的正是"荧光绿
当万能强调色"。所以绿不再是 accent：

- **结构（按钮 / 焦点环 / 选中态 / 硬线 / 链接 hover）→ 墨黑。** 无色相、
  不抢戏，层级靠字重与留白。
- **语义（成功 / 运行中 / 活跃）→ 绿。** 只在真要表达状态时出现，
  全站只有一个绿。
- **品牌（favicon / logo）→ 墨黑。** v0.8.3 全墨定案：品牌即骨架，只给品牌资产，不进交互。

判定标准：**一个颜色如果既出现在按钮上、又出现在状态点上，就是职责混淆。**

### 4.1 配色

| Token | 值 | 用途 |
|---|---|---|
| `--color-bg` | 纯白 `oklch(1 0 0)` | 全站底（v0.9.3 暖纸退役） |
| `--color-surface`~`surface-3` | `oklch(0.985 / 0.968 / 0.94 …265)` | 卡片、嵌套、hover |
| `--color-ink` / `ink-2` / `ink-3` | `oklch(0.21 / 0.442 / 0.63 …265)` | 主文字、次级、辅助 |
| `--color-line` / `line-strong` | `oklch(0.92 / 0.87 …265)` | 边框 |
| `--color-accent` | **墨黑** `oklch(0.21 0.008 265)` | 结构强调色，不是绿 |
| `--color-live` / `success` | 信号绿 `oklch(0.499…145)` | 运行中、成功 |
| `--color-brand` | **墨黑** `oklch(0.21 0.008 265)` | 仅 favicon / logo / 印章（v0.8.3 全墨） |
| `--color-spot` 色阶 | **品红** `oklch(0.70 0.26 350)` 四层 | 墨底荧光点睛色（v0.9.2，见 4.5） |
| `--shadow-xs`~`xl` | 五级 elevation | 卡片与浮层层次 |

中性灰阶**统一色相 hue 265、彩度 ≤ 0.017**。v0.6 的 ink(30°) / ink-2(55°) /
line(88°) / surface(90°) 四个色相混用，是界面泛脏的根源，已修正。

### 4.2 字体 — MiSans 一声部

主字体 MiSans，通过 jsDelivr CDN `@font-face` 内联各站 `globals.css`。
不用 next/font 加载 CJK 字体。

### 4.3 Logo — 云朵标

theme 包提供云朵 Logo（黑线资产）。favicon 定案（v0.9.2 满彩品红）：
**品红底 `#FF00A8`** + 白色云朵线条，rx 22% 圆角方形、16% padding、512px。
12 个文件（10 站 `app/icon.png` + theme 品牌源 + memory）同文件同 md5，
禁止自造文字/印章 svg。

favicon 是**品牌资产**。沿革：荧光绿 `#51D845`（v0.6，白底对比度仅 1.8:1，
且是 AI 生成站视觉陈词）→ 草本绿 `#1D7729`（v0.8.1）→ 墨黑 `#16181C`
（v0.8.3）→ **满彩品红 `#FF00A8`**（v0.9.2，品红点睛色阶定案，亮暗双底
均合格：墨底 4.92:1 / 白底 3.44:1，图形用途 ≥3:1 达标）。语义色（成功/
运行中=绿）是功能色，不随品牌色变动。

### 4.5 品红点睛色阶 — 墨底荧光（v0.9.2）

品牌重新有了彩色，但只干一件事：**在深色载体上做"行动 / 事件"的标记**。
单色相 H=350，四层 + ink：

| 层级 | 值 | 墨底对比 | 用途 |
|---|---|---|---|
| `--color-spot-strong` | `#FF00A8` | 4.92:1 | favicon、唯一主 CTA、单个图标（小字勿用） |
| `--color-spot` | `#FF42BB` | **5.71:1** | 正文 / 按钮 / 图表主线 |
| `--color-spot-soft` | `#FE8DC5` | 8.32:1 | 边框 / 辅助线 / 次要文字 |
| `--color-spot-deep` | `#C32E85` | 3.43:1 | 色块底 / 对比线，**禁承载文字** |
| `--color-spot-ink` | `= ink` | — | spot 实底上的文字色，恒为墨黑 |

四条铁律：

1. **墨底专用。** 只出现在 `#16181C` / `#0E0F12` 上；白底上主色仅
   3.12:1，禁用（守卫 G1，code review 把关）。
2. **只配墨黑字。** 白字在满彩上 3.61:1、在主色上 2.06:1，全部不可读
   （守卫 G3，code review 把关）。
3. **与语义红不同框。** 品红距语义红仅 35°（OKLab ΔE 0.229），同一视口
   区块不得同时出现 spot 与 danger（守卫 G2，CI 强制）。
4. **spot-strong 每屏最多一次。** 它是事件色不是按钮色，一屏出现两次
   以上就退化成噪音。数据图表的"多少"归 spot 主色，"好坏"归语义色。

### 4.4 署名 motif — 牧之方印

「牧之」两字方印只作为作者署名出现：muzhi 站 footer、文章与课程末尾。
不参与主品牌视觉，不在导航栏出现。

## 5. 产品矩阵 — z·m·z·a·i 逐字母体系

`zmzai` 是「知末 + AI」的合成词。**五个字母各挂一条产品线**，既是产品索引，
又是品牌的拆解。子域用单字母：

| 子域 | 字母 | 中文名 | 产品线 |
|---|---|---|---|
| `z.zmzai.cloud` | Z | 沙箱 | Sandbox — 代码执行与 Agent 实验 |
| `m.zmzai.cloud` | M | 模型 | Relay — 模型与 API 中转站 |
| `zmzai.cloud`（主站） | H | 枢纽 | Hub — 品牌首页与产品索引 |
| `a.zmzai.cloud` | A | Agent | Agent — Agent 编排与工作流 |
| `i.zmzai.cloud` | I | 工作台 | WorkOS — 一体化 AI 工作台 |
| `muzhi.zmzai.cloud` | 牧 | 牧之 | 署名站 — 博客 + 付费知识体系 |

这套结构把"品牌名就是产品矩阵"变成锚点：五个字母是品牌名的拆解，别人
复制不了。

## 6. 枢纽架构

```
zmzai.cloud                ← 主站：品牌首页 + 产品索引
├── zmzai-agent            ← Agent 编排与工作流
├── zmzai-relay            ← 模型目录、额度钱包与服务间 API
├── zmzai-sandbox          ← 受限代码执行层
├── zmzai-auth             ← 产品线单点登录
├── zmzai-workos           ← 一体化 AI 工作台
├── muzhi                  ← 牧之署名站：博客 + 付费知识体系
└── zmzai-db / zmzai-theme ← 共享数据模型与设计系统
```

- 主站极薄：品牌首页 + `/projects` 索引 + 每个产品卡（名称、一句话、状态
  徽章、跳转）。
- 子项目共享同一底盘（字体、配色、云朵 Logo），集合里每个项目可识别，又
  明显是一家人。
- 域名用子域：`[x].zmzai.cloud`。子域对应独立部署。

## 6.5 产品命名（书写系列 · 2026-08-30 定案）

一页纸从写到印到装订到落款的完整过程，对应产品从执行到记录到归档的全链路。
全部为真实英文出版术语——开发者零学习成本（log / index / plate 在代码语境里本就有意义），
无中式印章元素。

| 对外名 | 产品 | 域名（第二阶段） | 隐喻 |
|---|---|---|---|
| **Folio** | 门户 · Cloud | zmzai.cloud | 对开本首页——账户、工作区、入口 |
| **Quill** | Agent 工作台 | quill.zmzai.cloud | 执笔者 |
| **Margin** | 模型中继 | margin.zmzai.cloud | 页边通道——批注与传递 |
| **Plate** | 代码沙箱 | plate.zmzai.cloud | 印版——隔离试跑，跑完即焚 |
| **Index** | 收件箱 · WorkOS | index.zmzai.cloud | 目录——今日视图 |
| **Codex** | 记忆中心 | codex.zmzai.cloud | 古卷——装订成册的记忆 |
| **Colophon** | 投研竞技场 | colophon.zmzai.cloud | 版本记——排行与落款 |
| muzhi | 内容站 | muzhi.zmzai.cloud | 保留现名 |

**命名与视觉同源**：纯白底 = Folio 的纸，墨骨 = Quill 的墨，Serif 标题 = Codex 的字。
名字解释视觉、视觉解释名字——这是品牌最难得的自洽，改动任一侧都要回看另一侧。

**规则**
- 技术标识（仓库名 / 包名 @zmzai/* / pm2 进程名 / deploy-targets）与品牌名是两套系统；
  品牌名只出现在用户看得见的地方（页面标题、导航子标、文档、对外沟通）。
- 新产品命名：从出版/书写词表里取，且必须能回答"它在这页纸的哪个位置"。
- 第一阶段只改呈现层（已完成）；第二阶段配 DNS 子域并 301 旧域（待排期）。

## 7. 品牌戒律（写代码时遵守）

这些不是建议，是判断标准。新页面/组件不满足，宁可不做：

1. **UI 一律用 @zmzai/theme 组件。** theme 缺的补充进 theme 包再发版消费，
   不要在项目里自造。
2. **不用 Inter/Geist 当唯一正文字体。** 主字体 MiSans，通过 CDN 加载，
   fallback 只能是无衬线——衬线进 sans 栈会让 CDN 挂掉时整站变宋体。
3. **结构用墨黑，语义用彩色，两者不许混。** 按钮、焦点环、选中态、硬线
   一律 `--color-accent`（墨黑）；只有"成功 / 运行中 / 告警 / 错误"这类
   真实状态才用彩色。不用靛蓝/紫渐变 hero，也不用荧光绿当万能强调色。
4. **一个颜色只干一件事。** 同一个绿不能既在按钮上、又在状态点上。
5. **不做居中对齐的通用落地页。** 用左对齐的 Editorial Index 结构。
6. **不做四张图标方块功能卡。** 要列能力就写成段落或编号清单。
7. **不在导航栏贴图形 logo 当装饰。** wordmark 在左，云朵 Logo 进品牌区，
   署名 motif 已随印章视觉下线（2026-08-30 定案）。
8. **层次用灰阶和阴影，不用彩色描边。** 卡片靠 `--color-surface` 与
   `--shadow-*` 分层，不靠 accent 描边去"点亮"。
9. **文字对比度 ≥ 4.5:1（状态点 ≥ 3:1）。** 荧光绿在白底只有 1.8:1，
   这类色只能做大面积色块，不能做正文和小字。
10. **空留白比烂装饰好。** "Better nothing than bad something"——一个项目
    达不到纸+墨+印系统，就发纯底、不上强调色，绝不引入第二套临时系统。
11. **Display 用衬线，正文用无衬线。** 页面 h1/h2/display 走 `--font-serif`
    （Noto Serif SC），正文/数字/代码保持 sans/mono。衬线只出现在标题层级，
    不下探正文——书卷气靠标题承担。
12. **底是纯白，层次靠灰阶和边框。** `--color-bg` = oklch(1 0 0)（v0.9.3
    用户定案，暖纸退役）。不靠底色造氛围——单调的解药是内容密度与衬线
    标题，不是把纸染黄。卡片用 `--color-surface` 灰阶 + `--color-line`
    边框分层；同样不做滚动进度条、章节水印、网格底纹这类"模板感装饰"。
13. **品红只活在墨底上，且每屏一次。** `--color-spot*` 色阶只能出现在
    深色载体，实底配墨黑字，与 danger 不同框（守卫 G2 强制）。
    白底界面永远保持墨骨单色——彩色的出口是深色叙事区，不是按钮。
14. **页面标题一律走 `PageHeader`，不许各页面手写 h1。** theme v0.9.4
    引入，2026-08-31 全面替换（此前 agent/relay 共 38 个页面各自手写，
    标题旁 0 图标、字号分 5 档、token 各有偏差）。两种 variant，没有第三种：
    - `page`——其余全部：内联图标（20px）+ text-2xl 衬线标题，眉标用
      分区名（「控制台」「长期上下文」），描述 text-sm `text-ink-2`。
    - `hero`——仅落地页：44px 墨底徽章 + text-4xl，居中构图加 `centered`。
    标题旁**必须有图标**，图标取 theme 语义图标集（16px 网格、currentColor），
    禁止 Emoji / Unicode 字符当图标。描述不用 `text-ink-3`（3.69:1 太低），
    也不用 `text-muted-foreground` 这类 shadcn 遗留变量。

## 8. 口吻

- 平台口吻是"知末智云"，作者署名是"牧之"。两者并存，不混用。
- 短句。陈述句。少形容词。
- 不卖技术词，讲闭环和为什么。不写"赋能"、"全栈"、"AI 驱动"。
- 公告和文档可以长，营销文案能短就短。

## 9. 它不是什么

- 不是多租户 SaaS。
- 不是 AI 工具聚合站。
- 不是通用 LMS。
- 不是"看不见作者的模板站"。
- 不是换皮的模板站源码。

它是知末智云把一套经过真实业务验证的闭环开源出来，署名牧之。
`zmzai.cloud` 是这套署名的门牌号。

## 10. 事实来源

- 视觉系统锁定文件：`@zmzai/theme` 包（Radix UI + framer-motion + Tailwind v4 + MiSans）。
- tokens 源：各站 `app/globals.css` 的 `:root`（即 `tokens.css`）。
- 产品矩阵数据：`lib/projects.ts`（z·m·z·a·i 逐字母体系）。
- 署名站：`muzhi`（部署于 muzhi.zmzai.cloud）。
- 本文件版本：3，2026-08-25。
